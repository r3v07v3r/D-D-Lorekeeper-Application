fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic-settings==2.5.2
python-dotenv==1.0.1

# python-multipart is required by FastAPI for form/file parsing (used by later
# routers that accept file uploads, e.g. audio); harmless to include now.
python-multipart==0.0.9

# Pycord voice extra. This pulls in PyNaCl, required for voice connections.
# FFmpeg must additionally be present on the system PATH at runtime for Discord
# voice send/receive to work - this is a system binary, not a pip package, and
# is NOT bundled by this requirements file. Confirm it is on PATH inside the
# PyInstaller-bundled executable during Phase 5 (risk #5) - a dev venv having
# ffmpeg on PATH does not guarantee the packaged app will.
py-cord[voice]==2.6.1

# AI pipeline (Phase 2): Whisper transcription + GPT-4o summarization.
openai==1.51.0

# D&D Beyond sync (Phase 3): HTTP client for the character-service API.
httpx==0.27.2

# Self-signed TLS certificate generation, so traffic (including the
# campaign passphrase and session tokens) is encrypted once the backend is
# reachable beyond the GM's own machine - see app/tls.py.
cryptography==43.0.3
