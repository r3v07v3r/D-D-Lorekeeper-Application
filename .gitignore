# Python
backend/venv/
__pycache__/
*.pyc
.pytest_cache/

# Runtime data
backend/*.db
recordings/
backend/recordings/
backend/cert.pem
backend/key.pem
backend/settings.json

# Env files
.env
backend/.env
frontend/.env.local

# PyInstaller build output
backend/build_tmp/

# Node / frontend
node_modules/
dist/
build/

# Electron packaging output
release/

# Built installers placed in the repo root for local testing. Deliberately
# not committed: the exe is ~106MB, over GitHub's hard 100MB per-file push
# limit - distribute installers via GitHub Releases instead (which is also
# what the auto-updater expects).
Lorekeeper-Setup-*.exe

